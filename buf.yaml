version: v2
lint:
  use:
    - STANDARD
    - COMMENT_ENUM
    - COMMENT_ENUM_VALUE
  except:
    - PACKAGE_VERSION_SUFFIX
modules:
  - path: .
    excludes:
      - shared/v1/validation
      - reference
deps:
  - buf.build/bufbuild/protovalidate
