version: v2
plugins:
  # Go only — Java plugins require local binaries not available in CI.
  - remote: buf.build/protocolbuffers/go
    out: .
    opt: module=github.com/channel-io/ch-proto-public
  - remote: buf.build/grpc/go
    out: .
    opt: module=github.com/channel-io/ch-proto-public
